/*
 * 
 */
package hmi.util;

/**
 * An SynchronizedExchangeBuffer is an ExchangeBuffer that, unlike ExchangeBuffer,
 * guarantees that its operations are synchronized. 
 */
public interface SynchronizedExchangeBuffer extends ExchangeBuffer {
    
}
